[build]
  publish = "public"
  command = "yarn build"
  functions = "lambda"

[build.environment]
  YARN_VERSION = "1.22.4"
  YARN_FLAGS = "--no-ignore-optional"
  NODE_VERSION = "20.19.0"
  NODE_OPTIONS = "--openssl-legacy-provider"
